# Evals

The benchmark checks whether retrieval returns the curated source notes for each synthetic clinical question.

Run it after the API is up:

```bash
python evals/run_benchmark.py --base-url http://localhost:8000
```

The resulting percentage is the only number that should be used in a resume bullet or demo narrative.

